@using AnimalRepair.BLL.DTO;
@model Animal_Repair.Models.KindOfGenderIndexModel

@{
    ViewData["Title"] = "Index";
    var names = Model.KindOfGenders;
}
<p>
    <a a class="nav-link text-dark" asp-action="Create" asp-controller="KindOfGender">Create New</a>
</p>

<div class="cards">
    @foreach (var item in Model.KindOfGenders)
    {
        <div class="cardItem">
            <p>
                @Html.DisplayFor(modelItem => item.Gender)
            </p>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
            <a asp-action="Details" asp-route-id="@item.Id" asp-route-GenderName="@item.Gender ">Details</a>
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </div>
    }
</div>

