@using AnimalRepair.BLL.DTO;
@model Animal_Repair.Models.KindOfProductIndexModel

@{
    ViewData["Title"] = "Index";
    var names = Model.kindOfProducts;
}
<p>
    <a a class="nav-link text-dark" asp-action="Create" asp-controller="KindOfProduct">Create New</a>
</p>

<div class="cards">
    @foreach (var item in Model.kindOfProducts)
    {
        <div class="cardItem">
            <p>
                @Html.DisplayFor(modelItem => item.Name)
            </p>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
            <a asp-action="Details" asp-route-id="@item.Id" asp-route-GenderName="@item.Name ">Details</a>
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </div>
    }
</div>

